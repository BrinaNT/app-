# 多端审发使用手册

## 一、软件用途

**多端审发** 用于管理 App 在多个应用市场的审核状态查询、打包、上传和发布。

目前支持：

- 查询各平台审核 / 上架状态
- 选择已有 APK 发布到 Android 应用市场
- 从 Flutter 项目一键打包并发布
- iOS 上传 App Store Connect 并提交审核
- Android 多市场发布
- 查看发布日志和失败原因
- 导入 / 导出项目配置

## 二、项目配置

使用前需要先添加项目。

项目配置主要包括：

- 项目名称
- 应用包名 / Bundle ID
- 各平台 App ID
- 各平台 API 凭证
- 启用的发布平台

包名非常重要。发布前程序会检查 APK 包名或 iOS Bundle ID 是否和项目配置一致，不一致会停止发布。

## 三、平台配置

进入项目后，可以为不同平台录入 API 参数。

Android 市场包括：

- 华为应用市场
- 荣耀应用市场
- 小米应用商店
- OPPO 软件商店
- vivo 应用商店
- 腾讯应用宝
- 三星应用商店

iOS 使用 App Store Connect API，需要配置：

- Apple App ID
- Issuer ID
- Key ID
- p8 私钥

其中 iOS 上传和提交审核主要依赖：

- Issuer ID
- Key ID
- p8 私钥

## 四、实时查询

点击 **实时查询** 后，程序会查询当前项目在已启用平台的状态。

查询过程中：

- 所有按钮不可点击
- 查询完成后按钮恢复
- 查询失败的平台会显示失败信息
- 详细日志会写入日志文件

如果某个平台超时，通常是接口连接慢或网络不稳定，不一定是参数错误。

## 五、选择 APK 发布到市场

这个功能适合你已经有一个打包好的 APK，只想发布到 Android 市场。

操作流程：

1. 点击 **选择 APK 发布到市场**
2. 选择本地 APK 文件
3. 勾选要发布的平台
4. 填写版本更新说明
5. 选择发布方式
6. 点击发布

发布前会检查：

- APK 包名是否和项目配置一致
- APK versionCode 是否高于线上版本
- 所选平台是否已接入发布 API

发布过程中：

- 每个平台独立执行
- 某个平台失败不会影响后续平台
- 成功或失败都会写入日志
- 发布结束后会弹窗显示各平台结果

## 六、一键发布

一键发布适合从 Flutter 项目源码开始，自动完成 iOS 和 Android 的打包与发布。

当前完整流程：

1. 检查项目地址
2. 检查 Flutter 项目
3. 检查 Android 包名
4. 根据选择设置发布环境
5. 执行一次 `flutter pub get`
6. 打包 iOS IPA
7. 上传 IPA 到 App Store Connect
8. 打包 Android APK
9. 检查 APK 包名和 Build 号
10. 发布到所选 Android 市场
11. Android 流程结束后，轮询 App Store Build 状态
12. 更新 App Store 版本说明
13. 关联本次上传的 Build
14. 提交 iOS 审核
15. 弹窗显示最终结果

## 七、一键发布环境要求

一键发布需要本机具备开发环境：

- Flutter
- Android SDK
- Xcode
- CocoaPods
- App Store 上传工具
- Android 签名配置
- iOS 签名配置

如果环境缺失，点击一键发布后会在进度弹窗和失败弹窗中显示错误原因。

## 八、发布环境 appMode

一键发布弹窗中有一个 **发布环境** 选项。

默认选中：

**自动设置发布环境**

它的作用是：在正式打包前，程序会检查你选择的 Flutter 项目，并在项目的 `lib` 目录下查找名为 `api.dart` 的文件。

如果找到了 `api.dart`，程序会把里面类似下面的配置：

```dart
appMode = 0
```

或：

```dart
appMode = 1
```

自动改成：

```dart
appMode = 2
```

这里的 `appMode = 2` 表示使用正式发布环境，避免打包时还停留在测试环境或开发环境。

如果没有找到 `api.dart`，一键发布会停止，并在进度和失败弹窗里提示原因。

如果你已经手动把项目切到正式发布环境，可以选择：

**我已手动设置**

这样程序不会修改 `api.dart`，系统默认你发布的是正式环境。

## 九、iOS 版本要求

App Store 上传要求版本号必须高于已审核通过版本。

例如线上已通过版本是：

```text
1.18.0
```

那么新版本不能还是：

```text
1.18.0
```

应该改为：

```text
1.18.1+64
```

或：

```text
1.19.0+64
```

其中：

- `1.18.1` 是 iOS 版本号
- `64` 是 Build 号

## 十、App Store 自动提交审核

iOS 上传成功后，程序会等待 Android 发布流程完成，然后开始轮询 App Store Build。

轮询规则：

- 每 30 秒查询一次
- 最多查询 40 次
- 最长等待约 20 分钟

Build 可用后，程序会：

- 查找或创建对应 App Store 版本
- 更新版本说明
- 关联本次上传的 Build
- 创建审核提交
- 提交审核

如果 App Store 缺少截图、隐私、合规等资料，Apple 会返回错误，程序会写入日志。

## 十一、Android 发布规则

Android 发布时：

- 会先检查 APK 包名
- 会检查 versionCode 是否高于线上版本
- 每个平台独立发布
- 单个平台失败不会中断其他平台
- Android 失败不会阻止 iOS 提交审核

如果某个平台失败，最终弹窗会显示失败原因，日志中也会保留详细错误。

## 十二、日志位置

日志会存放在系统为 App 分配的应用数据目录中。

macOS：

```text
~/Library/Application Support/com.genius.checkappstatus/appLog
```

Windows：

```text
%APPDATA%\com.genius.checkappstatus\appLog
```

日志文件类似：

```text
app_logs_2026-09-04.log
```

这里的 `~` 表示当前登录用户的主目录，`%APPDATA%` 表示 Windows 当前用户的应用数据目录。

日志会记录：

- 查询过程
- 发布步骤
- 打包输出
- API 请求失败原因
- App Store 上传结果
- Android 各平台发布结果

日志可以删除，删除后只会影响历史记录查看，不影响发布功能。

## 十三、常见问题

### 1. IPA 上传成功，但 App Store 后台没马上看到？

正常。App Store Connect 处理 Build 需要时间，通常几分钟到十几分钟。

### 2. IPA 打包好了，为什么 Xcode Archives 看不到？

`flutter build ipa` 生成的 archive 在项目目录：

```text
build/ios/archive/Runner.xcarchive
```

不一定出现在 Xcode Organizer 的 Archives 列表中，不影响上传。

### 3. App Store 提示版本号不高？

说明 `pubspec.yaml` 中的版本号没有大于线上已审核版本，需要提升版本号。

### 4. 三星查询超时怎么办？

平台接口超时时间为 30 秒。如果仍然超时，通常是三星接口或网络连接不稳定，可以稍后重试。

### 5. Android 某个平台发布失败，会影响 iOS 吗？

不会。Android 某个平台失败后会继续发布其他平台，最后仍然会尝试提交 iOS 审核。
